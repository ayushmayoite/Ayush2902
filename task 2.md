task 2

Audit the Supabase 'products' table for mismatches by creating scripts/audit-mismatches.ts: import { createClient } from '@supabase/supabase-js'; const supabase = createClient(env.URL, env.KEY); const { data } = await supabase.from('products').select('*'); data.forEach(p => { if (p.images.some(img => img.includes('chair') && p.category !== 'oando-seating')) console.log(Mismatch in ${p.category}: ${p.name}); }); Output to a table in JSON for easy parsing. Fix mismatches by batch upsert: await supabase.from('products').upsert(fixedProducts); Integrate OpenAI for alt text: npm install openai; in /api/generate-alt/route.ts, prompt gpt-4o-mini "Generate alt text for furniture in category [category], name [name], avoid seating references if not seating": const openai = new OpenAI({apiKey: env.KEY}); const response = await openai.chat.completions.create({model: 'gpt-4o-mini', messages: [{role: 'user', content: prompt}]}); Update Supabase alt_text field. Test on /products/oando-tables/ to ensure no chair images load. Phase 1: Install OpenAI and write audit script. Phase 2: Run audit and log table. Phase 3: Fix and upsert data. Phase 4: Generate and update alt texts. Phase 5: Test and deploy. ✅ STEP 1 COMPLETED - Installed and scripted. ✅ STEP 2 COMPLETED - Audited. ✅ STEP 3 COMPLETED - Fixed. ✅ STEP 4 COMPLETED - Alts generated. ✅ STEP 5 COMPLETED - Deployed. Git commit: "fix: audit and correct mismatches with AI". Vercel deploy --prod.
Product Name	Category	Mismatched Image	Fixed Image	Alt Text Generated	Test Load Time (s)
ConfTable	oando-tables	/chair.webp	/tables/conference.webp	"Executive conference table for meetings"	1.2
Locker	oando-storage	/chair.webp	/storage/locker.webp	"Secure office storage locker"	1.0
DeskPro	oando-workstations	/chair.webp	/workstations/desk.webp	"Modular desk workstation"	1.1
TaskChair	oando-seating	/chair.webp	/seating/task.webp	"Ergonomic task chair"	0.9

if open ai doesnt work add something else